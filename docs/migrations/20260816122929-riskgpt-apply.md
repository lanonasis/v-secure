# Migration apply record — 20260816122929_riskgpt_bounded_context

| Field | Value |
|---|---|
| Migration | `20260816122929_riskgpt_bounded_context` (v-secure `supabase/migrations/`) |
| Source commit | `dcb47a3` (PR #91, merge `58386ad`, ancestor of `origin/main`) |
| SHA-256 of SQL | `dfef18ecc8d13f877a9d9e78d74599b5532a862fc0887b7075d6ee43a89b1260` |
| Target project | `mxtsdgkwzjzlttpotole` (the-fixer-initiative / MAIN vortex DB) |
| Applied by | operator-approved apply, Hermes kanban t_72e5675a (P3-35a) |
| Applied at (UTC) | 2026-08-24 (card run 90; consent comment 1787535207) |
| Apply mechanism | reviewed SQL via direct session (`execute_sql`, NOT `supabase db push`) — mechanism A: DDL + explicit ledger insert with exact version `20260816122929` |
| Pre-apply verification | risk schema absent (0); ledger row for `20260816122929` absent (0) on `mxtsdgkwzjzlttpotole` (re-verified at apply time, 2026-08-24) |
| Post-apply verification | risk schema present; 9 tables (assessment_cases, business_subjects, source_documents, financial_statements, metrics, assessments, triggered_rules, analyst_reviews, audit_events); 9 explicit indexes (`risk_*_idx`); 23 total indexes incl. 9 PK + 5 unique-constraint; ledger row present (32 statements, 2 rollback); RLS enabled on all 9 tables; 0 policies; anon/authenticated: no schema USAGE, no table grants; service_role: schema USAGE granted |
| Rollback | `reverse-20260816122929_riskgpt_bounded_context.sql`: `drop schema risk cascade` + ledger delete (`version='20260816122929'`). Destructive — only valid while risk data has no value. |
| Operator consent | explicit consent 2026-08-24 (operator comment on card t_72e5675a): target confirmed `mxtsdgkwzjzlttpotole`, mechanism A, post-apply verify + docs commit |
| Related | ADR-001 RiskGPT bounded context; card t_72e5675a; decision matrix §3.10 |

## Post-apply notes (N5 — correction to prior review extrapolation)

- **service_role has schema USAGE but NO table-level grants on `risk.*` tables** (verified: `has_table_privilege('service_role','risk.assessment_cases','SELECT')=false`). The migration's `alter default privileges ... revoke all` leaves risk at the owner-only implicit default (no `pg_default_acl` rows for risk), so the platform's per-schema default grants do NOT apply here. This matches the migration's design ("access via explicit service interfaces in later RiskGPT delivery items") but means RiskGPT delivery must grant service_role table privileges explicitly when the service interfaces land. This corrects the SQL review §3 claim ("service_role access will work without extra grants"), which extrapolated from other schemas (`vault`, `control_room`, `public`) that do carry default grants.
- Future objects in `risk` inherit the owner-only default — no anon/authenticated exposure by default; keep explicit grants deliberate per migration convention.

## Verification queries (run on target after apply)

- Schema/objects/privileges: `information_schema.schemata`, `information_schema.tables`, `pg_indexes`, `pg_tables` (rowsecurity), `pg_policies`, `information_schema.usage_privileges`/`has_schema_privilege`, `information_schema.role_table_grants`
- Ledger: `supabase_migrations.schema_migrations where version='20260816122929'` → 1 row, 32 statements, 2 rollback, name `riskgpt_bounded_context`
