# Example environment file (do not put real secrets here)

# Allow http:// endpoints (local development only; prefer HTTPS)
VORTEX_ALLOW_INSECURE_HTTP=0

# Optional hostname allow-list (comma-separated hostnames or host:port entries)
# VORTEX_ALLOWED_HOSTS=api.vortex-secure.com,localhost:4056

# Optional TLS hardening (recommended for production)
# VORTEX_TLS_CA_FILE=/path/to/custom-ca.pem
# VORTEX_TLS_PINNED_SPKI_SHA256=sha256/REPLACE_WITH_BASE64URL_PIN

# SDK configuration (used by createMCPClientFromEnv)
VORTEX_ENDPOINT=https://api.vortex-secure.com
VORTEX_MCP_TOKEN=replace-me
MCP_TOOL_ID=replace-me
MCP_TOOL_NAME=Example Tool
MCP_TOOL_VERSION=0.1.0
