/**
 * Owns deterministic financial metric calculations and their input contracts.
 * Formula implementation is intentionally deferred to RG-003.
 */
export const metricsEngineBoundary = 'metrics-engine' as const;
